<%= form_with(model: store_seller, local: true) do |form| %>
  <% if store_seller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(store_seller.errors.count, "error") %> prohibited this store_seller from being saved:</h2>

      <ul>
      <% store_seller.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
